#include <mcu.h>
#include <rumble.h>
#include <packet.h>

#include <stdio.h>

int main()
{
    uint8_t buffer[64];     // Buffer for an output packet
    uint8_t timer = 0;      // Timer for the output packets, incr by 1 for each sending

    // TODO: Connect and do nothing

    return 0;
}
